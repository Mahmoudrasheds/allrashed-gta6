[build]
  command = "echo 'Build complete'"
  functions = "functions"
  publish = "."

[dev]
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
